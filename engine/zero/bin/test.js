// Generated by CoffeeScript 1.7.1
(function() {
  var expect, results, _deter, _fails, _tests;

  _tests = [];

  _fails = 0;

  _deter = function() {};

  expect = function(a) {
    return {
      a: a,
      to: function(b) {
        var determ;
        determ = _deter(a, b);
        if (determ) {
          return _tests.push(true);
        } else {
          _tests.push(false);
          console.log("Test(" + _tests.length + ") failed: (" + a + ")[" + b + "]");
          return _fails = _fails + 1;
        }
      }
    };
  };

  results = function() {
    console.log("Passed: " + (_tests.length - _fails) + "/" + _tests.length);
    if (_fails === 0) {
      return console.log("All passed!");
    }
  };

}).call(this);
