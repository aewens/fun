// Generated by CoffeeScript 1.7.1
(function() {
  var Grid;

  Grid = (function() {
    function Grid(world, w, h) {
      this.paint = world.paint;
      this.line = world.line;
      this.w = w;
      this.h = h;
      this.unit = pow(2, 4);
    }

    Grid.prototype.blocks = function(unit) {
      return this.unit = unit;
    };

    Grid.prototype.render = function() {
      var x, xu, y, yu, _i, _j, _ref, _ref1, _results;
      for (y = _i = 1, _ref = this.h / this.unit - 1; 1 <= _ref ? _i <= _ref : _i >= _ref; y = 1 <= _ref ? ++_i : --_i) {
        yu = y * this.unit;
        this.line(0, yu, this.w, yu);
      }
      _results = [];
      for (x = _j = 1, _ref1 = this.w / this.unit - 1; 1 <= _ref1 ? _j <= _ref1 : _j >= _ref1; x = 1 <= _ref1 ? ++_j : --_j) {
        xu = x * this.unit;
        _results.push(this.line(xu, 0, xu, this.h));
      }
      return _results;
    };

    return Grid;

  })();

  window.Grid = Grid;

}).call(this);
